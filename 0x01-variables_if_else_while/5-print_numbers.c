#include <stdio.h>
/**
 * main - prints numbers fro 0 to 9
 *
 * Return: Always 0 (code is working)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
